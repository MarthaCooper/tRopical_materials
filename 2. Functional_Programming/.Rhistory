pagedown::chrome_print("path/to/your/slides.Rmd")
pagedown::chrome_print("ppt_md.Rmd")
apply(mtcars, 2, mean)
my_means <- apply(mtcars, 2, mean)
my_means
?lapply
as.list(mtcars)
mtcars_list <- as.list(mtcars)
mtcars_list
head(mtcars_list)
head(mtcars_list, 2)
my_means <- lapply(mtcars_list, mean)
my_means
head(my_means, 2)
mtcars_sums <- apply(mtcars, 2, sum)
mtcars_sums <- apply(mtcars, 2, sum)
mtcars_sums
